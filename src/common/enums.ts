export enum TaskStatus {
  Completed = 'completed',
  InProgress = 'inProgress',
}

export enum SortOrder {
  ASC = 'asc',
  DESC = 'desc',
}

export enum ActionsMode {
  ADD = 'add',
  UPDATE = 'update',
}
